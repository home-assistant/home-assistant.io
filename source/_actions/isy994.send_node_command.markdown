---
title: Send node command
action: isy994.send_node_command
domain: isy994
description: "Sends a command to an ISY node using its Home Assistant entity."
related_actions:
  - isy994.send_raw_node_command
  - isy994.send_program_command
---

With this action, you can send a command to an ISY node using its Home Assistant entity. This gives you control over actions that are not otherwise exposed in Home Assistant, such as making a device beep or fading a light.

{% include actions/ui_header.md %}

To use this action in an automation or script:

1. Go to {% my automations title="**Settings** > **Automations & scenes**" %}.
2. Open an existing automation or script, or select **Create** to start a new one.
3. If you're setting up a new automation, add a trigger in the **When** section. Scripts don't need a trigger.
4. In the **Then do** section, select **Add action**.
5. Select what you want to control. Under **By target** (see [Targets](#targets)), select the ISY entity you want to control. You can also select an area, a device, or a label.
6. From the actions shown for that target, select **ISY: Send node command**.
7. In the **Command** field, select the command you want to send.
8. Select **Save**.

### Options in the UI

{% options_ui %}
Command:
  description: "The command to send to the node. One of `beep`, `brighten`, `dim`, `disable`, `enable`, `fade_down`, `fade_stop`, `fade_up`, `fast_off`, `fast_on`, or `query`."
{% endoptions_ui %}

{% include actions/yaml_header.md %}

In YAML, refer to this action as `isy994.send_node_command`. A basic example looks like this:

{% example %}
action: |
  action: isy994.send_node_command
  target:
    entity_id: light.front_porch
  data:
    command: fast_on
{% endexample %}

This sends the `fast_on` command to the `light.front_porch` node.

### Options in YAML

{% options_yaml %}
command:
  description: "The command to send to the node. One of `beep`, `brighten`, `dim`, `disable`, `enable`, `fade_down`, `fade_stop`, `fade_up`, `fast_off`, `fast_on`, or `query`."
  required: true
  type: string
{% endoptions_yaml %}

<a id="targets"></a>
<a id="targets-of-the-action"></a>

## Targets of the action

This action requires a target. The target is the object of the action. You can point the action at a single {% term entity %}, a device, an area, a floor, or a label, and Home Assistant runs the action on every matching ISY entity behind that target.

- **Entity**: one specific ISY entity, such as `light.front_porch`.
- **Device**: every ISY entity that belongs to a device.
- **Area**: every ISY entity in a room or area.
- **Floor**: every ISY entity on a floor.
- **Label**: every ISY entity that shares a label.

You can also select different target types in one action. For example, you can add a specific entity and an area as targets in the same action to run the action on both of them at once.

{% include actions/try_it.md %}

{% include actions/stuck.md %}

{% include actions/related.md %}
